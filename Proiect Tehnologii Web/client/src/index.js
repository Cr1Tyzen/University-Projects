import React from 'react';
import { createRoot } from 'react-dom';
import App from './Pagini/App'; 

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
